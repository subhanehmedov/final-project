spring:
  application:
    name: subhan-final-project
  datasource:
    url: jdbc:postgresql://localhost:5432/payment_system_db
    username: postgres
    password: salman
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none